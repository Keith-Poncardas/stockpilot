/**
 * Sortable columns for stock movement list queries.
 * Values map to Prisma field names (camelCase).
 */
export enum StockMovementOrderBy {
    CREATED_AT = "createdAt",
    QUANTITY = "quantity"
}
